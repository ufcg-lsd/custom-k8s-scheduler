apiVersion: v1
kind: Pod
metadata:
  name: low-priority-pod
  labels:
    app: test-app
  annotations:
    controller: "test-controller2"  # Annotation necessária para controllerName
spec:
  priorityClassName: free-priority
  containers:
  - name: nginx
    image: nginx:latest
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: "8000m"
        memory: "268Mi"
      limits:
        cpu: "8000m"
        memory: "268Mi"
  schedulerName: custom-scheduler
